# Goal:
- Create for reinforcement learning the app with the name `pendulum` with the gym environment `Pendulum-v1`, to run live with selectable methods and compare methods with a live reward plot.

## Very important: 
- !!! use `..\rl_init.md` for inital requirements !!!

## Additional Features
- Selectable methods (dropdown): dqn, dueling ddqn, noisy dqn,  distributional dqn
- Selectable parameters: seed and as options: low (default -7), high (default -5)