# Goal:
- Create for reinforcement learning the app with the name `card_pole` with the gym environment `CartPole-v1`, to run live with the current or in compare mode the latest of the selectable methods and compare methods with a reward plot live to each developing method.

## Very important: 
- !!! use `..\rl_init.md` for inital requirements !!!

## Additional Features
- Selectable methods (dropdown): dqn, ddqn
- Gravity, Force, Seed, Frame interval (ms)
